version: 2

project_name: cyber-community

before:
  hooks: []

builds:
  - id: cyber
    main: ./cmd/cyber
    binary: "cyber"
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    flags:
      - -trimpath
    tags:
      - forceposix
      - emptytemplates
      - noembed
      - osusergo
      - netgo
    ldflags:
      - >-
        -s -w
        -X 'github.com/chainreactors/cyber/core/config.Version={{.Version}}'
        -X 'github.com/chainreactors/cyber/core/config.DefaultProvider=deepseek'
        -X 'github.com/chainreactors/cyber/core/config.DefaultAPIKey={{ .Env.COMMUNITY_LLM_KEY }}'
        -X 'github.com/chainreactors/cyber/core/config.DefaultModel=deepseek-chat'
        -X 'github.com/chainreactors/cyber/core/config.DefaultCyberhubURL={{ .Env.COMMUNITY_CYBERHUB_URL }}'
        -X 'github.com/chainreactors/cyber/core/config.DefaultCyberhubKey={{ .Env.COMMUNITY_CYBERHUB_KEY }}'
        -X 'github.com/chainreactors/cyber/core/config.DefaultCyberhubMode=merge'
        -X 'github.com/chainreactors/cyber/core/config.DefaultVerify=auto'
        -X 'github.com/chainreactors/cyber/core/config.DefaultTavilyKeys={{ .Env.COMMUNITY_TAVILY_KEYS }}'
    asmflags:
      - all=-trimpath={{.Env.GOPATH}}
    gcflags:
      - all=-trimpath={{.Env.GOPATH}}

  - id: cyber-agent
    main: ./cmd/agent
    binary: "cyber-agent"
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    flags:
      - -trimpath
    tags:
      - forceposix
      - emptytemplates
      - noembed
      - osusergo
      - netgo
    ldflags:
      - >-
        -s -w
        -X 'github.com/chainreactors/cyber/core/config.Version={{.Version}}'
        -X 'github.com/chainreactors/cyber/core/config.DefaultProvider=deepseek'
        -X 'github.com/chainreactors/cyber/core/config.DefaultAPIKey={{ .Env.COMMUNITY_LLM_KEY }}'
        -X 'github.com/chainreactors/cyber/core/config.DefaultModel=deepseek-chat'
        -X 'github.com/chainreactors/cyber/core/config.DefaultCyberhubURL={{ .Env.COMMUNITY_CYBERHUB_URL }}'
        -X 'github.com/chainreactors/cyber/core/config.DefaultCyberhubKey={{ .Env.COMMUNITY_CYBERHUB_KEY }}'
        -X 'github.com/chainreactors/cyber/core/config.DefaultCyberhubMode=merge'
        -X 'github.com/chainreactors/cyber/core/config.DefaultVerify=auto'
        -X 'github.com/chainreactors/cyber/core/config.DefaultTavilyKeys={{ .Env.COMMUNITY_TAVILY_KEYS }}'
    asmflags:
      - all=-trimpath={{.Env.GOPATH}}
    gcflags:
      - all=-trimpath={{.Env.GOPATH}}

upx:
  - enabled: true
    goos: [linux, windows]
    goarch:
      - amd64

archives:
  - id: cyber
    builds: [cyber]
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
    formats:
      - zip
    files:
      - none*

  - id: cyber-agent
    builds: [cyber-agent]
    name_template: "{{ .ProjectName }}-agent_{{ .Os }}_{{ .Arch }}"
    formats:
      - zip
    files:
      - none*

checksum:
  name_template: "{{ .ProjectName }}_checksums.txt"

snapshot:
  version_template: "{{ incpatch .Version }}-community"

changelog:
  sort: desc
  filters:
    exclude:
      - '^MERGE'
      - "{{ .Tag }}"
      - "^docs"

release:
  disable: true
