syntax = "proto3";

package cyber.reload;

import "types/config.proto";

option go_package = "github.com/chainreactors/cyber/pkg/types;types";

message ReloadRequest { cyber.config.DistributeConfig config = 1; }

message ReloadResult {
  bool ok = 1;
  string provider = 2;
  string model = 3;
  string error = 4;
}

message ReloadProtocolMessage {
  oneof message {
    ReloadRequest request = 10;
    ReloadResult result = 11;
  }
}
